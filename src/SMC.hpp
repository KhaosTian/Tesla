#pragma once

#include "Common.h"

namespace gpu {
class StreamingMultiprocessorController {
public:
  StreamingMultiprocessorController();
  ~StreamingMultiprocessorController();
};
} // namespace gpu